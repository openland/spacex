overwrite: true
schema: "./schema.graphql"
documents: "./queries/queries.graphql"
generates:
  types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  schema.json:
    plugins:
      - "introspection"
